{% extends ":admin:index.html.twig"%}

{% block body %}
      {% block nb %}   
      {% endblock%}
{% block workweb %}
    {% endblock%}
 {% block workprint %}
        {% endblock%}
 {% block workgalerie %}
        {% endblock%}

     <div class="project-content">
         {% block ariane %}
             <ol class=" ariane-admin breadcrumb">
  <li><a href="{{path('admin')}}">Home</a></li>
  <li  class="active"> Liste print</li>
</ol>
             {% endblock %}
   <div class="container project-admin">
        <div class="row">
       {% for projetprint in projectsprint %}
   
    <div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-heading"><a class="lien-projet-web" href="{{path("detailprojetsprint",{"id":projetprint.id})}}"><i class="icon-add fa fa-desktop"></i>{{projetprint.titre}}</a></div>

  <!-- Table -->
  <table class="table">
      <thead> <tr>
              <th>id</th>
              <th>description</th>
              <th>technique</th>
              <th>publier</th>
          </tr>
            <td>{{projetprint.id}}</td>
            <td>{{projetprint.description|truncate(10, false, ' ...')}}</td>
            <td>{{projetprint.technique}}</td>
            {% if projetprint.publier ==[1] %}
             <td><i class="fa fa-check"></i></td>
             {% else %}
         <td><i class="fa fa-times"></i></td>
         {% endif %}
         </thead>
  </table>
</div>

 
 {% endfor %}
 </div>
</div>
 
     </div>
{% endblock%}


