{% extends ":admin:index.html.twig"%}

{% block body %}
        <a href="{{path("formdetail")}}"><h1 class="add-work">ajouter detail web</h1> </a>
        <div class="project-content"> 
      <div class="container project">
        <div class="row">
       {% for projet in projects %}
   
      <div class="col-sm-6 col-md-6">
      <img src="{{asset('uploads/img/')~projet.images}}" class="img-responsive img">
        <p class="">{{projet.titre}}</p>
     
      <p class="">{{projet.descriptif}}</p>
      <p class="">{{projet.liens}}</p>
      <div class="btn-projet">
            <a class="btn btn-primary" href="{{path("modifdetail",{"id":projet.id})}}">Modifier</a>
            <a class="btn btn-danger" href="{{path("delatedetail",{"id":projet.id})}}">Supprimer</a>
            </div>
    </div>
  


 
 {% endfor %}
 </div>
      </div>
</div>
{% endblock%}

