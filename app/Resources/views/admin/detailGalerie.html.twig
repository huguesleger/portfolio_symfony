{% extends ":admin:index.html.twig"%}

{% block body %}
       {% block nb %}   
      {% endblock%}
{% block workweb %}
    {% endblock%}
 {% block workprint %}
        {% endblock%}
 {% block workgalerie %}
        {% endblock%}

      
     <div class="project-content">
     
         
   <div class="container project">
       
         
        <div class="row">
       {% for galerie in projectsgal %}
   
      <div class="col-sm-6 col-md-12">
      <img src="{{asset('uploads/img/')~galerie.imageMin}}" class="img-responsive img">
        <h2 class="">{{galerie.titre}}</h2>
     
      <p class="">{{galerie.description}}</p>
      <p class="">{{galerie.technique}}</p>
       <img src="{{asset('uploads/img/')~galerie.imageDetail}}" class="img-responsive img">
        <img src="{{asset('uploads/img/')~galerie.imageDetail1}}" class="img-responsive img">
         <img src="{{asset('uploads/img/')~galerie.imageDetail2}}" class="img-responsive img">
     
 
      <div class="btn-projet">
            <a class="btn btn-primary" href="{{path("modifgal",{"id":galerie.id})}}">Editer</a>
            <a class="btn btn-danger" href="{{path("delategal",{"id":galerie.id})}}"data-confirm="Tes sur de vouloir supprimer?">Suppr</a>
            </div>
           
    </div>


 
 {% endfor %}
 

 </div>
</div>


     </div>
{% endblock%}



