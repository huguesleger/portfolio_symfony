{% extends ":site:index.html.twig"%}

{% block section %}
    <div class="container-fluid image-print">
        <div class="row">
       {% for projetprint in projectsprint %}
  
      <div class="col-sm-12 col-md-12">
    
      <img src="{{asset('uploads/img/')~projetprint.images}}" class="img-responsive">
     
        <p class="thumb-title">{{projetprint.titre}}</p>

      
      <p class="thumb-title">{{projetprint.description}}</p>
      <a class="fa fa-plus-circle" href="{{path("detailprint",{"id":projetprint.id})}}">En voir +</a>
    
    </div>
   
 

 
 {% endfor %}
  </div>

</div>


  {% block javascript %}
     {% endblock%}
     
{% endblock%}
